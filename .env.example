# Copy to .env.local and fill in. .env.local is never committed.

# Pooled Postgres connection used by the app at runtime.
DATABASE_URL=

# Direct, unpooled Postgres connection used by prisma migrate.
DIRECT_URL=
